interface SlotAnchorProps {
  name: string;
  context?: unknown;
  className?: string;
}

export const SlotAnchor = ({ name, context, className }: SlotAnchorProps) => {
  void name;
  void context;
  void className;
  return null;
};
